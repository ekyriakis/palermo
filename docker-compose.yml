version: '3.8'

services:
  nightinplaermo:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    # Optional: add resource limits
    # mem_limit: 512m
    # cpus: '0.5'
